{
  "name": "ariva-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "ARIVA — mobile live kitchen (plain static site)",
  "scripts": {
    "dev": "live-server --port=3000 --open=/ariva-website.html --ignore=node_modules,.git --wait=500"
  },
  "devDependencies": {
    "live-server": "^1.2.0"
  }
}
