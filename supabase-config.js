/* ============================================================
   ARIVA — Supabase keys   (THE ONLY PLACE YOU PASTE YOUR KEYS)
   ============================================================
   Both the main site (ariva-website.html) and the login page
   (account.html) read your keys from this one file.

   STEP 1: Replace YOUR_SUPABASE_URL with your Project URL.
   STEP 2: Replace YOUR_SUPABASE_ANON_KEY with your "anon public" key.

   You'll find both inside Supabase:  Project Settings → API.
   (Full walkthrough is in README — steps 2 and 3.)

   SAFETY: Only ever paste the "anon public" key here.
   NEVER paste the "service_role" key into this file — it is a
   secret admin key and must never go into a website.
   ============================================================ */

window.SUPABASE_URL      = 'YOUR_SUPABASE_URL';
window.SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
